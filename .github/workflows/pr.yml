name: Events Demo 1
on:
  push:
    branches:
      - develop
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Output event data
        run: echo "${{ toJSON(github.event) }}"
      - name: Get code
        uses: actions/checkout@v3
      - name: Deploy project
        run: echo "Deploying to test environment..."
  create_pull_request:
    needs: deploy
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    # - name: Create Pull Request
    #   uses: peter-evans/create-pull-request@v3
    #   with:
    #     token: ${{ secrets.GITHUB_TOKEN }}
    #     commit-message: Automated Pull Request
    #     title: Automatically creating pull request to staging
    #     body: This pull request was automatically created when code was pushed to the develop branch.
    #     branch: staging
    #     base: develop
        
    - name: open-pull-request
    # You may pin to the exact commit or the version.
    # uses: rematocorp/open-pull-request-action@65f5854e8ca6e493c6ee5c2d6202d1c5d2c60569
      uses: rematocorp/open-pull-request-action@v1
      with:
        # Github token, https://docs.github.com/en/actions/reference/authentication-in-a-workflow
        github-token: ${{ secrets.GITHUB_TOKEN }}
        # head branch
        from-branch: develop
        # base branch
        to-branch: staging
        # 
        repository-owner: sreenak
        # 
        repository: DevopsTest
          
